{#
/**
 * @file sigmaxim_workflow_order.html.twig
 * Default theme implementation to present Products data.
 *
 * This template is used when viewing Products pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_sigmaxim_workflow_order()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('sigmaxim_workflow_order') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
